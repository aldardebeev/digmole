// import { Worker } from 'bullmq';
// import { EQueue } from "../../libs/queues/queue.enum"

// const worker = new Worker(EQueue.START_BOT, async job => {
//     worker.on('failed', job => {
//         console.log(`${job.id} has completed!`);
//     });
//     worker.on('completed', job => {
//         console.log(`${job.id} has completed!`);
//     });
   
// });